# Packwatch

Downlaod the submodules using the command:
```shell
git submodule update --init --recursive
```

To launch the game:
```shell
love .
```

To turn on grid, press `g` in game

To turn off grid, press `g` in game